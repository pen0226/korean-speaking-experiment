# =====================================================
# AI 기반 한국어 말하기 피드백 시스템 - 간소화 버전 (Excel + tiktoken 제거)
# Korean Speaking AI Feedback System - Simplified Version (No Excel + tiktoken)
# =====================================================

# === 핵심 웹 프레임워크 (필수) ===
streamlit==1.31.1
streamlit-mic-recorder==0.0.8

# === AI API 서비스 (필수) ===
openai==1.12.0
elevenlabs==0.2.27

# === 토큰 최적화 제거 (문자 수 기반 처리로 대체) ===
# tiktoken==0.6.0  # 제거: Streamlit 에러 방지

# === PDF 생성 (동의서) ===
reportlab==4.1.0

# === Google Cloud Storage 백업 시스템 (필수) ===
google-auth==2.28.2
google-cloud-storage==2.14.0

# === 환경변수 (로컬 개발용) ===
python-dotenv==1.0.1

# =====================================================
# 🚀 간소화된 기능 (Excel 변환 제거로 용량 절약):
#
# ✅ 모든 핵심 기능 유지:
# - 음성 녹음 및 인식 (Whisper API)
# - AI 피드백 생성 (GPT-4o)
# - 음성 합성 (ElevenLabs TTS)
# - Google Cloud Storage 자동 백업
# - PDF 동의서 생성
# - 긴 텍스트 처리: 문자 수 기반 (토큰 기반 대신)
# - 로컬 개발 환경 지원
#
# ❌ 제거된 기능:
# - Excel 파일 변환 (pandas, openpyxl 제거)
# - 토큰 기반 텍스트 처리 (tiktoken 제거)
# - 연구자는 CSV 파일만 다운로드 (Excel에서 쉽게 열 수 있음)
# - 문자 수 기반 텍스트 처리 (기능적으로 동일)
#
# 📊 용량 비교:
# - 이전: ~150MB (pandas + tiktoken 포함)
# - 현재: ~75-85MB (약 45% 감소)
# - 배포 시간: 2-3분 → 약 1분
#
# 🔧 영향:
# - 연구자: CSV 파일을 Excel에서 열어서 사용
# - 학생: 완전히 동일한 경험
# - 자동 백업: 완전히 동일한 기능
# - 긴 텍스트 처리: 문자 수 기반 (기능적으로 동일)
#
# Streamlit Cloud Secrets 설정:
# - OPENAI_API_KEY
# - ELEVENLABS_API_KEY
# - ELEVEN_VOICE_ID
# - gcp_service_account (JSON)
# - GCS_BUCKET_NAME
# - GCS_ENABLED=true
# =====================================================